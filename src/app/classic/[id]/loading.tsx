import ClassicImportLoadingUI from "./ClassicImportLoadingUI";

/** Shown during initial navigation while the `[id]` segment is preparing. */
export default function Loading() {
  return <ClassicImportLoadingUI />;
}
